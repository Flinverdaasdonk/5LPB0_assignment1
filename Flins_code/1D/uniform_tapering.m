function [a] = uniform_tapering(N_x)
    % UNIFORM_TAPERING: Takes in the number of elements, returns a 
    % INPUT
    %   N_x: scalar, number of elements
    % OUTPUT
    %   a: row vector, ones
    a = ones(1, N_x);
end

